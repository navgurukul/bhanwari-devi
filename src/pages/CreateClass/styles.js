import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  successModel: {
    alignItems: "center",
    align: "center",
  },
  confirmationModal: {
    marginTop: "300px",
    padding: "20px",
    borderRadius: "8px",
  },
}));

export default useStyles;
