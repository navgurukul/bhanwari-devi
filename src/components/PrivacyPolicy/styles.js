import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  underline: {
    width: "60px",
    textAlign: "center",
    marginBottom: "30px",
  },
  list: {
    display: "flex",
    marginLeft: "12px",
  },
}));

export default useStyles;
