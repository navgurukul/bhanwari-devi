import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  courseNumber: {
    background: "#E9F5E9",
    width: "20px",
    height: "20px",
    borderRadius: "50%",
    padding: "5px",
    marginRight: "20px",
  },
}));

export default useStyles;
