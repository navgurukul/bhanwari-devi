import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
  usertext: {
    textAlign: "center",
  },
  userBtn: {
    height: "50%",
    width: "20%",
  },
}));
export default useStyles;
